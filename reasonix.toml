[permissions]
allow = ["explore", "Edit", "Bash(cd /Users/lex/code/github/argus && cargo clippy -p argus-tui 2>&1 && cargo fmt --check -p argus-tui 2>&1)", "Bash(cd /Users/lex/code/github/argus && cargo test -p argus-tui 2>&1)", "Bash(cd /Users/lex/code/github/argus && git add -A && git commit -m \"fix(tui): preserve directory Cyan color when filter is active\n\nWhen the fuzzy text filter is active, directories were losing their\nCyan color and appearing as White (same as files). This made the\nfilesystem tree harder to navigate.\n\nFix both branches in name_spans:\n- Non-matching items: check is_dir() and use Cyan instead of White\n- Matching items (match_highlight_spans): accept is_dir param, use\n  Cyan as normal_fg for directories, matched chars still highlighted\n  in Green\")"]
